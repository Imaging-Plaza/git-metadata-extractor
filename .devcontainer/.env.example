# Copy to `.devcontainer/.env` for docker-compose variable substitution (host port mappings).
# Compose reads this file from the `.devcontainer/` directory, not the repo-root `.env`.
#
# SSH_PORT=2220
# APP_PORT=1234
# DEV_PORT=8888
